info:
  name: Update Member
  type: http
  seq: 8

http:
  method: PUT
  url: http://127.0.0.1:5000/api/members/1
  body:
    type: json
    data: |-
      {
          "position": "Treasurer"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
