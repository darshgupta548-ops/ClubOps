info:
  name: New Resource
  type: http
  seq: 20

http:
  method: POST
  url: http://127.0.0.1:5000/api/resources
  body:
    type: json
    data: |-
      {
        "event_id" : 1,
        "name" : "Soft Boards",
        "quantity" : 4,
        "condition" : "Need to replace"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
