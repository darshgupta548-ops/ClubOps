info:
  name: Update Event
  type: http
  seq: 12

http:
  method: PUT
  url: http://127.0.0.1:5000/api/events/1
  body:
    type: json
    data: |-
      {
          "venue":"OSAIL Ground"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
