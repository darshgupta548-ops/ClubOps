info:
  name: New Club
  type: http
  seq: 1

http:
  method: POST
  url: http://127.0.0.1:5000/api/clubs/
  body:
    type: json
    data: |-
      {
        "name": "Brahmand",
        "description": "Astronomy Club",
        "faculty_advisor": "Dr. XYZ"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
